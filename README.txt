SELECT PLACEHOLDER FIX — v45

Only two files are changed:
- InfrastructureInput.tsx
- NetworkInput.tsx

Fix:
- Removes the duplicated Infrastructure/Réseau text.
- Empty placeholder is pale and font-weight 400 (not bold).
- Selected value is dark, sharp and font-weight 600.
- Floating MUI label remains pale and font-weight 400.
- No other project files or option icons are changed.
